import 'package:flutter/widgets.dart';

class MyDutiesHomeLoadingIndicator extends StatelessWidget {
  const MyDutiesHomeLoadingIndicator({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}